node_modules/
dist/
.vite/

# TanStack Router generiert die routeTree pro Build/Dev neu — wir checken
# sie nicht ein, sonst gibt es Merge-Konflikte bei jedem Route-File-Change.
src/routeTree.gen.ts

# Lokale .env (Frontend-Env, falls je gebraucht)
.env.local
