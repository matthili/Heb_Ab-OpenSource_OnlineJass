node_modules/
dist/
.vite/

# TanStack Router temporäre Files (vom router-plugin angelegt)
.tanstack/

# Playwright-Artefakte
playwright-report/
test-results/
playwright/.cache/

# TanStack Router generiert die routeTree pro Build/Dev neu — wir checken
# sie nicht ein, sonst gibt es Merge-Konflikte bei jedem Route-File-Change.
src/routeTree.gen.ts

# Karten-Assets werden bei predev/prebuild aus assets/cards/ gesynct.
public/cards/

# Lokale .env (Frontend-Env, falls je gebraucht)
.env.local
