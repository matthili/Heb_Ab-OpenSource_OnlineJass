# dependencies
node_modules/
.pnpm-store/

# build artifacts
dist/
build/
.next/
.astro/
.turbo/
*.tsbuildinfo

# test/coverage
coverage/
.nyc_output/
playwright-report/
test-results/

# env / secrets
.env
.env.*
*.env
!.env.example
!.env.tunnel.example
secrets/

# editor / OS
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# NN-Artefakt — wird per `pnpm sync:nn` aus dem Schwester-Repo gezogen
external/jass-nn/

# Prisma generated client
apps/api/prisma/generated/

# misc
*.tmp
*.bak
*.bundle
.claude/

# lokal-private Arbeitsdokumente — bleiben lokal, gehören nicht ins öffentliche Repo
/PLAN.md
/RECOVERED_PLAN.md
/RECOVERED_CONVERSATION.md

# TLS / Zertifikate / Keystores (falls beim Self-Hosting lokal im Repo-Pfad abgelegt)
*.pem
*.key
*.crt
*.pfx
*.p12
*.jks
*.keystore

# Cloudflare Tunnel — Credentials enthalten echte Tokens
infra/cloudflared/*.json
cloudflared-config.yml
