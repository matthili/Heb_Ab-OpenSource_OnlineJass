{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jass-app.serviceAccountName" . }}
  labels:
    {{- include "jass-app.labels" . | nindent 4 }}
{{- end }}
